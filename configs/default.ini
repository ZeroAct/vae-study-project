[model]
input_size = [256, 256]
in_channel = 3
latent_dim = 100
hidden_dims = [64, 64, 128, 128, 256, 256]

[hyperparameters]
epochs = 5
batch_size = 2
optimizer = adam
learning_rate = 1e-2

[data]
train_img_path = "datasets/train"
val_img_path = "datasets/val"

